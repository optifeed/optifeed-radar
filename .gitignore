# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# local run state (never commit; may contain user data, never keys)
.optifeed/

# local secrets - engine API keys (never commit)
.env
.env.local

# coverage
coverage/

# editor / OS
.DS_Store
*.log
.idea/
.vscode/
